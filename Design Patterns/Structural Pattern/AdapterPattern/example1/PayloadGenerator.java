// Target interface expected by the client for generating payloads
public interface PayloadGenerator {
    
    // Method to generate the payload in the desired format (e.g., JSON)
    String generate();
}
