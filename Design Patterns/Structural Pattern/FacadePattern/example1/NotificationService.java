// NotificationService defines a contract for sending notifications to users.
public interface NotificationService {

    // This method is used to send a notification with the given message.
    void notifyUser(String message);
}
